import * as React from 'react';

interface IShareProps {
}

const Share: React.SFC<IShareProps> = (props) => {
  return  (
      <div>
          Sharing Widget Here
      </div>
  );
};

export default Share;